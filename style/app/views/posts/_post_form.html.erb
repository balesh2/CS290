<%= simple_form_for(@post, :html => {:class =>'form-horizontal'}) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photo %>
  <%= f.hidden_field :users_id, :value => current_user.id %>
  <%= f.button :submit %>
<% end %>

